import { HomePage } from "@/components/landing/home-page";

export default function MarketingLanding() {
  // Show the marketing landing page without any redirects
  // This will be accessible via /welcome route
  return <HomePage />;
}